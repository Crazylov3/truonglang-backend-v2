# Development Configuration
# Overrides base.yaml settings for development environment

app:
  debug: true

database:
  host: "localhost"

redis:
  host: "localhost"

cors:
  origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
    - "http://127.0.0.1:3000"
    - "http://127.0.0.1:8080"

email:
  from_email: "dev@learnify.com" 